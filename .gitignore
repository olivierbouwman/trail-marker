# Runtime output (the live log lives in your RDR2 folder, not the repo).
# Note: viewer/TrailMarker.bin is committed on purpose as sample data.
*.log

# IDE
.idea/

# Claude Code local config
.claude/

# macOS
.DS_Store

# Nexus release artifact (regenerable from TrailMarker.asi + nexusmods-zip-readme.txt)
TrailMarker-*.zip

# Browser-automation tool output (Playwright MCP snapshots/logs)
.playwright-mcp/
